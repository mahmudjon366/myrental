# ===========================================
# RENTACLOUD BACKEND ENVIRONMENT VARIABLES
# ===========================================

# Database Configuration
# Local MongoDB (Development)
MONGO_URL=mongodb+srv://username:password@cluster.mongodb.net/rentacloudorg?retryWrites=true&w=majority

# MongoDB Atlas (Production) - Uncomment and configure for cloud database
# MONGO_URL=mongodb+srv://username:password@cluster.mongodb.net/rentacloudorg?retryWrites=true&w=majority

# Server Configuration
PORT=4000
NODE_ENV=development

# Security Configuration
# Generate a strong JWT secret (minimum 32 characters)
# You can use: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-here-minimum-32-characters-for-production-security

# JWT Token Expiration (default: 7d)
JWT_EXPIRES_IN=7d

# Admin User Configuration (for seeding initial admin user)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change-this-password-in-production-immediately

# CORS Configuration
# Development
CORS_ORIGIN=http://localhost:5173

# Production (uncomment and set your domain)
# CORS_ORIGIN=https://your-domain.com

# Multiple origins (comma-separated)
# CORS_ORIGIN=https://your-domain.com,https://www.your-domain.com

# Logging Configuration
LOG_LEVEL=info

# Rate Limiting (requests per minute per IP)
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=100

# File Upload Configuration (if needed in future)
# MAX_FILE_SIZE=5242880
# UPLOAD_PATH=./uploads

# Email Configuration (if needed for notifications)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# Backup Configuration
# BACKUP_ENABLED=true
# BACKUP_SCHEDULE=0 2 * * *
# BACKUP_RETENTION_DAYS=30

# Performance Configuration
# DB_CONNECTION_POOL_SIZE=10
# DB_CONNECTION_TIMEOUT=30000

# Security Headers
# HELMET_ENABLED=true
# TRUST_PROXY=true

# Development/Debug Configuration
# DEBUG=rentacloud:*
# VERBOSE_LOGGING=false